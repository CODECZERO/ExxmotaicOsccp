"""
echo-n — OCPP debug echo server package (default: 2.0.1).

Provides version-specific WebSocket server factories that delegate
all handler logic to the ``core`` library.  Used for smoke-testing
physical charger hardware without a database or business logic.
"""
